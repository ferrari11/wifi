# Vercel Ready

This project is configured as a static HTML/CSS/JavaScript site for Vercel.

## Vercel settings

- Framework Preset: Other
- Build Command: `npm run build`
- Output Directory: `.`
- Install Command: `npm install`

## Local verification

```bash
npm install
npm run build
```

Because this is a static site, no Node server is required for production.
